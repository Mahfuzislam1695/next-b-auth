/* eslint-disable @typescript-eslint/no-unused-vars */
import { IUserEditProps } from "@/types/user/user.types";

export default function EditUser({ setEditModalOpen, refetch }: IUserEditProps) {
    // export default function EditUser({ setEditModalOpen, refetch }: IUserEditProps) {
    return (
        <div>EditUser</div>
    )
}
